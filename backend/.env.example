PG_USER=your_user_name      # Replace with your PostgreSQL username
PG_PASSWORD=your_password   # Replace with your PostgreSQL password
PG_HOST=your_host           # Replace with your PostgreSQL host (e.g., localhost or a remote server)
PG_PORT=5432                # PostgreSQL default port (change if necessary)
PG_DATABASE=postgres        # Replace with your PostgreSQL database name